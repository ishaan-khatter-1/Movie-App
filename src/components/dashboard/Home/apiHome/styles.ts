import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'row',
    marginVertical: 10,
    marginHorizontal: 20,
  },
  titleStyle: {
    fontSize: 18,
    color: 'black',
  },
});

export default styles;
