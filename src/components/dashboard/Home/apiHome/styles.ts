import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  mainContainer: {
    // flexDirection: 'row',
  },
  container: {
    flex: 1,
    // flexDirection: 'row',
    marginVertical: 10,
    marginHorizontal: 20,
  },
  imgOneStyle: {
    width: 100,
    height: 100,
  },
  titleStyle: {
    fontSize: 14,
    color: '#fff',
  },
});

export default styles;
