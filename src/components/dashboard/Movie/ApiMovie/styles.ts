import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  mainContainer: {
    alignItems: 'center',
  },
  imgOneStyle: {
    // width:
    aspectRatio: 1 / 1.5,
    marginHorizontal: 10,
  },
  imageStyle: {
    borderRadius: 20,
  },
});

export default styles;
