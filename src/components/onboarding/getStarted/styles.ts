import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  activedot: {
    backgroundColor: '#fff',
    height: 10,
    width: 10,
    borderRadius: 5,
    borderWidth: 1,
  },
});

export default styles;
